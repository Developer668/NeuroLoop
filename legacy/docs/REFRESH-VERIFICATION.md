# UI and MCP refresh — September 11, 2026

**Historical first-refresh report.** The follow-up now includes fresh CPU TRIBE and TSAM execution, an actual Molab notebook, a substantially rebuilt landing page and the restored Brain Lab layout. See [FRESH-EXECUTION.md](FRESH-EXECUTION.md) for current results. Counts and statements below describe the earlier snapshot.

The Windows app is running for local media work and evidence review. GPU inference remains paused. This update does not clear production or scientific release gates.

## Changes shipped locally

- Brain Lab now places the source player beside the cortical surface, with recorded stimulus timestamps, playback/slider synchronization, open/close hemispheres and a fixed full-record numeric color scale. Signed response uses blue/neutral/orange-yellow; absolute magnitude uses dark/red/orange/yellow and explicitly hides the sign.
- The surface is Three.js-rendered fsaverage5 anatomy: 10,242 vertices per hemisphere, not individual neurons. The time slider selects saved response segments, not inference latency. Runtime is separately displayed in provenance.
- Sidebar light/dark appearance persists locally. The crash hold is a compact sidebar/header status; enforcement remains unchanged.
- Upload has its own page. Library contains searchable assets; overview shows projects and actual counts. Settings groups appearance, execution, model and integration state.
- Neuro AI uses the supplied flat artwork and a compact real local-command interface. No free-form AI conversation is claimed. MCP remains the external agent entrypoint.
- MCP configuration has Codex CLI, generic desktop stdio JSON and authenticated HTTP JSON, with syntax coloring. Both transport paths use the same domain services.
- Landing has a larger automatically rotating non-draggable brain, an evidence navigation section, colored MCP sequence, a compact sponsor strip without the pause button and a smaller footer without the workspace bubble. The meme was preserved.
- marimo includes seven explained experiment plots and saved hemisphere timeline/heatmap views. Empty data stays empty.

## Fresh execution evidence

The old live library and projects were cleared at the user's request after a consistent **569-file backup** at `artifacts/workspace-before-refresh-20260912/`. Old media files remain on disk for recovery but are no longer listed in the live database. Remote W&B history was not deleted. See `artifacts/workspace-reset-receipt.json`.

The refreshed live database contains **2 projects, 3 assets, 0 runs and 0 evaluations**:

1. TIDELINE: a fictional bottle creative generated by Codex's image tool, uploaded through the real authenticated HTTP asset endpoint.
2. A six-second video composed from that image through the actual MCP `render_creative` tool and CPU FFmpeg, then assigned to an MCP-created project.
3. A fresh six-second trim of retained NASA Apollo descent footage, uploaded and assigned to another MCP-created project. This contains actual motion footage, not a synthetic test pattern.

The image-generation step belongs to Codex; NeuroLoop's own Ideogram/MiniMax generation adapters remain unconfigured. MCP composition is real media rendering, not text-to-image generation.

Receipt: `artifacts/refresh-verification/mcp-media-receipt.json`. Fresh stdio MCP initialized and discovered **21 tools**, exercised capabilities, composition, project creation, workspace readback and system status. A real `evaluate_creative` request was rejected by the graphics hold before queue insertion.

## Verification

| Check | Observed result |
|---|---|
| Backend suite | 198 passed, 3 platform skips, 2 dependency-deprecation warnings |
| Final frontend production build | Passed compilation, TypeScript and route generation |
| Edge HTTP MCP test | Real authenticated handshake; 21 tools discovered |
| Edge media playback | Apollo player readyState 4, duration 6 seconds; playback advanced to 4.47 seconds without a media error |
| Edge Neuro command | `/status` returned the actual empty run/evaluation ledger and active execution hold |
| Edge UI | Checked dark/light appearance, final dark contrast fixes, focused upload page, clean library, settings, desktop JSON, landing/footer and open hemispheres |
| Local marimo | Browser rendered real empty states after cleanup; no fabricated chart rows |
| Historical research charts | All 7 experiment charts and 58 cortical plots serialized from 29 historical evaluations; summaries matched saved prediction arrays |
| Historical Brain Lab | Isolated copied database on temporary ports 8012/3020; real saved evaluation `cdfaacce-e365-4eca-8447-ba323c64d5e5` rendered the magnitude heatmap in light/dark themes; slider value 1 sought media to 1.0 seconds; completed video playback selected final recorded segment 5 |
| Browser console | No captured error entries on landing and isolated populated Brain Lab |

The temporary historical preview was stopped and its tab closed after verification. Historical data was never imported back into the live workspace. Chart evidence is in `artifacts/refresh-verification/research-chart-check.json`. No neural inference or model loading was performed in this refresh.

## Remaining limits

- The recurrent Windows 0x113 crash is still undiagnosed; no repeat GPU stability claim is justified.
- TSAM assets and strict-loading implementation exist; this audit did not perform a fresh Windows forward pass. Class mapping, held-out metrics and calibration remain open.
- Kragel assets and volume projection exist, but a valid fsaverage-to-MNI registration and TRIBE transfer validation are not established. Finite scores and full coverage are insufficient.
- ARIA has provided a real review; the full local Launch experiment has not passed acceptance after the crash. W&B Inference, cloud compute/storage and TypeSafe remain deferred.
- The loop has genuine bounded edits, acceptance/revert rules, policy statistics and evidence. It is not neural self-training, proven preference optimization or autonomous driver repair.

See [current integration audit](CURRENT-INTEGRATION-AUDIT.md) for verified sponsor trace links, model hashes, plotting interpretation and the thumbnail-correlation limitations.
