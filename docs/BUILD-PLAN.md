> Historical plan/verification. Current recovery status and actual completed work are in [AUDIT.md](AUDIT.md) and [REQUEST-RECONCILIATION.md](REQUEST-RECONCILIATION.md). GPU execution is currently paused.

# NeuroLoop completion plan — 10 September 2026

All three `D:/Project NeuroLoop*.md` documents were read in full. Their examples are requirements and illustrative numbers, not application data. The latest user direction takes precedence: local inference, trusted locally quantized official TRIBE weights, no additional reward models, no cloud compute, and no fabricated integrations or psychological claims.

## Delivery sequence

1. Preserve existing assets, evidence and model weights. Organize the web application under `frontend/src`; keep the Python API and worker under `backend/neuroloop`. Update launch/build references.
2. Establish an original navy/teal identity with the requested GPT Image logo. Replace the landing page and workspace styling, navigation, terminology, information hierarchy and responsive behavior. Use anatomy-based 3D with selectable surface, points and wireframe views, hemisphere separation, explicit response scale, and reduced-motion support.
3. Keep the workflow grounded in uploaded creatives, project contracts, actual evaluations, controlled experiments and exportable evidence. Surface real activity charts and useful tables. Make MCP connection instructions concrete and copyable.
4. Inspect TSAM checkpoint structure against the original inference implementation. Resolve safe loading, class ordering and preprocessing before enabling any output. Investigate Kragel spatial registration against the exact TRIBE output geometry. Keep methodological uncertainty explicit.
5. Review local inference isolation, timeout/cancellation, memory limits and Windows crash evidence. Verify bounded actual inference, recording peak allocation and acknowledging the limits of a single successful run.
6. Verify backend tests, frontend build, actual API/MCP workflow and visual interaction checks. Record remaining external blockers rather than claiming that unavailable credentials or unvalidated scientific transfers are complete.

## Design references and decisions

Reviewed 21st.dev, Vue Bits, Aceternity, Uiverse, GSAP, Motion, Animate.css and Anime.js. Their scroll transitions, restrained depth and interaction feedback inform original components; no proprietary template is copied. Prefer native CSS and existing Three.js to multiple overlapping animation runtimes. The supplied X post could not be retrieved by web browsing. Edge browser control fails during the supplied tool bootstrap (`node:process` import disallowed), before a browser connection is established.

## Acceptance boundaries

- All visible numerical results must come from saved evidence or live system state.
- A cortical vertex is a surface sample, not an individually observed neuron.
- Reference similarity is not a human preference, conversion or purchase probability.
- No model training or RLHF is performed. Bandit statistics describe experiment outcomes.
- Optional external integrations must expose their actual configured/tested state.
- Windows graphics driver stability cannot be guaranteed by Python memory controls or one passing test.


## Execution update

The browser plugin bootstrap remained unavailable. The user explicitly authorized a separate temporary Edge test session. Verification therefore uses disposable Edge contexts with software WebGL; the user's normal browser session and account data were not read or changed.

Implemented: source organization, generated identity, redesigned landing/workspace, activity charts, library search, preferences, live device telemetry, on-demand 3D controls, guarded response differences, Destrieux region readout, experimental CPU TSAM, registered MCP tools, real MCP handshake/configuration download, full evidence export and read-only inference headroom checks. Removed unused alternative UI modules from the active tree, preserving them in `data/build-backups/retired-ui-20260910`.

Unresolved external/scientific dependencies: Kragel registration and transfer validation; graphics-kernel root-cause diagnosis; external sponsor credentials and ARIA/Launch configuration. No cloud compute was configured or used.
