> Historical plan/verification. Current recovery status and actual completed work are in [AUDIT.md](AUDIT.md) and [REQUEST-RECONCILIATION.md](REQUEST-RECONCILIATION.md). GPU execution is currently paused.

# Local release work plan

Latest scope: preserve the current identity and anatomical brain. Implement local
engineering, useful research visualizations, Weave and bounded ARIA integration.
W&B Inference, TypeSafe, cloud compute/storage, Kragel and TSAM scientific validation
are deferred by the latest user request. Public hosting is not requested.

1. Read all three project documents and seven pasted attachments. Treat older
   roadmap examples as proposals, not measured outputs or permission to add models.
2. Preserve the original environments and database. Build isolated runtimes with
   explicit locks and audit dependencies. Switch only after model/API tests pass.
3. Add versioned database migrations, consistent backup/restore rehearsal and live
   capability checks. Investigate available graphics-crash evidence read-only.
4. Store Weave publication status, identifiers, errors and bounded retries durably.
   Connect the user's account and verify visible remote records when browser/account
   access is available. Add official W&B MCP using its supported auth flow.
5. Add a versioned ARIA proposal contract, evidence export and local execution
   adapter through the existing queue. No remote arbitrary code or budget bypass.
6. Expand marimo: cortical traces, region/time heatmap, comparison matrix,
   cost/gain scatter, operator outcome chart, parallel coordinates and run timelines.
   Use real records, shared filters, readable explanations and honest empty states.
7. Adapt the supplied path/footer/composer/loading designs to existing colors.
   Neuro supports actual local evidence commands; AI model replies remain deferred.
   Remove demo models, simulated voice, fake app-store links and unverified claims.
8. Verify isolated installs, migrations/restore, delivery/proposal failure paths,
   actual model outputs, both MCP transports and Edge interactions. Publish concrete
   receipts and keep external/scientific blockers visible.

Edge is connected through the browser extension. A private/team NeuroLoop W&B
project was created. Credential authorization is being checked through the normal UI.
